server:
  port: 8080
etag:
  hashAlgorithm: "MD5"
spring:
  redis:
    host: localhost
    port: 6379
    database: 0
  jackson:
    date-format: "yyyy-MM-dd HH:mm:ss"
    time-zone: "America/New_York"
jsonSchema:
  location: "/json/jsonSchema.json"
logging:
  level:
    root: info